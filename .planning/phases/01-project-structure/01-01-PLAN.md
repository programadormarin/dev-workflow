---
phase: 01-project-structure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dev_workflow/__init__.py
  - src/dev_workflow/domain/__init__.py
  - src/dev_workflow/domain/models/__init__.py
  - src/dev_workflow/domain/models/jira_ticket.py
  - src/dev_workflow/domain/models/github_pr.py
  - src/dev_workflow/infrastructure/__init__.py
  - src/dev_workflow/infrastructure/adapters/__init__.py
  - src/dev_workflow/infrastructure/adapters/jira_adapter.py
  - src/dev_workflow/infrastructure/adapters/github_adapter.py
  - src/dev_workflow/infrastructure/adapters/logging_adapter.py
  - tests/__init__.py
  - tests/unit/__init__.py
  - tests/unit/domain/__init__.py
  - tests/unit/domain/models/__init__.py
  - tests/unit/infrastructure/__init__.py
  - tests/unit/infrastructure/adapters/__init__.py
autonomous: true
requirements:
  - STRU-01
  - STRU-02
  - STRU-04

must_haves:
  truths:
    - "Developer can import domain models with `from src.dev_workflow.domain.models import JiraTicket`"
    - "Developer can import adapters with `from src.dev_workflow.infrastructure.adapters import JiraAdapter`"
    - "Domain models use Pydantic for data contracts (per CONTEXT.md decision)"
    - "Infrastructure adapters implement port protocols (dependency inversion)"
  artifacts:
    - path: "src/dev_workflow/domain/models/jira_ticket.py"
      provides: "JiraTicket Pydantic model with all required fields"
    - path: "src/dev_workflow/domain/models/github_pr.py"
      provides: "GitHubPR Pydantic model"
    - path: "src/dev_workflow/infrastructure/adapters/jira_adapter.py"
      provides: "JiraAdapter stub implementing JiraPort"
    - path: "src/dev_workflow/infrastructure/adapters/github_adapter.py"
      provides: "GitHubAdapter stub implementing GitHubPort"
    - path: "tests/unit/domain/models/test_jira_ticket.py"
      provides: "JiraTicket unit tests"
  key_links:
    - from: "src/dev_workflow/infrastructure/adapters/jira_adapter.py"
      to: "src/dev_workflow/ports/__init__.py"
      via: "import JiraPort"
    - from: "src/dev_workflow/infrastructure/adapters/jira_adapter.py"
      to: "src/dev_workflow/domain/models/jira_ticket.py"
      via: "import JiraTicket"
---

<objective>
Scaffold the clean architecture foundation: domain models and infrastructure adapters. Creates all __init__.py stubs, Pydantic domain models (JiraTicket, GitHubPR), and infrastructure adapter stubs with proper imports. Mirrors src/ structure into tests/.

Purpose: Establish the domain model and infrastructure layers per the clean architecture decision in CONTEXT.md (Sequential execution, Pydantic models for data contracts).
Output: Domain layer with models, infrastructure layer with adapters, mirrored test structure
</objective>

<context>
@.planning/ROADMAP.md (Phase 1 success criteria and STRU requirements)
@.planning/phases/01-project-structure/01-CONTEXT.md (clean architecture decisions)
@pyproject.toml (existing project config)
</context>

<interfaces>
<!-- Key types defined in this plan that downstream plans will import -->

From src/dev_workflow/domain/models/jira_ticket.py:
```python
from pydantic import BaseModel, Field
from enum import Enum

class IssueType(str, Enum):
    STORY = "Story"
    BUG = "Bug"
    TASK = "Task"

class JiraTicket(BaseModel):
    key: str = Field(description="Jira ticket key, e.g. PROJ-123")
    summary: str
    description: str
    issue_type: IssueType
    status: str
    assignee: str | None = None
    acceptance_criteria: list[str] = Field(default_factory=list)
    comments: list[str] = Field(default_factory=list)
    linked_issues: list[str] = Field(default_factory=list)
```

From src/dev_workflow/domain/models/github_pr.py:
```python
from pydantic import BaseModel, Field

class GitHubPR(BaseModel):
    number: int
    title: str
    body: str
    head_branch: str
    base_branch: str = "main"
    status: str
    url: str
    ticket_key: str | None = Field(default=None, description="Jira ticket key if linked")
```

From src/dev_workflow/infrastructure/adapters/jira_adapter.py:
```python
from src.dev_workflow.domain.models import JiraTicket
from src.dev_workflow.ports import JiraPort

class JiraAdapter(JiraPort):
    """Jira Cloud REST API adapter — stub to be implemented in Phase 3."""
    ...
```

From src/dev_workflow/infrastructure/adapters/github_adapter.py:
```python
from src.dev_workflow.domain.models import GitHubPR
from src.dev_workflow.ports import GitHubPort

class GitHubAdapter(GitHubPort):
    """GitHub CLI/API adapter — stub to be implemented in Phase 3."""
    ...
```
</interfaces>

<must_haves>
  truths:
    - "Developer can import domain models with `from src.dev_workflow.domain.models import JiraTicket`"
    - "Developer can import adapters with `from src.dev_workflow.infrastructure.adapters import JiraAdapter`"
    - "Domain models use Pydantic for data contracts (per CONTEXT.md decision)"
    - "Infrastructure adapters implement port protocols (dependency inversion)"
  artifacts:
    - path: "src/dev_workflow/domain/models/jira_ticket.py"
      provides: "JiraTicket Pydantic model with all required fields"
      min_lines: 25
    - path: "src/dev_workflow/domain/models/github_pr.py"
      provides: "GitHubPR Pydantic model"
      min_lines: 15
    - path: "src/dev_workflow/infrastructure/adapters/jira_adapter.py"
      provides: "JiraAdapter stub implementing JiraPort"
      min_lines: 10
    - path: "src/dev_workflow/infrastructure/adapters/github_adapter.py"
      provides: "GitHubAdapter stub implementing GitHubPort"
      min_lines: 10
    - path: "tests/unit/domain/models/test_jira_ticket.py"
      provides: "JiraTicket unit tests"
      min_lines: 15
  key_links:
    - from: "src/dev_workflow/infrastructure/adapters/jira_adapter.py"
      to: "src/dev_workflow/ports/__init__.py"
      via: "import JiraPort"
      pattern: "from src.dev_workflow.ports import JiraPort"
    - from: "src/dev_workflow/infrastructure/adapters/jira_adapter.py"
      to: "src/dev_workflow/domain/models/jira_ticket.py"
      via: "import JiraTicket"
      pattern: "from src.dev_workflow.domain.models import JiraTicket"
</must_haves>

<tasks>

<task type="auto">
  <name>task 1: Create domain layer with Pydantic models</name>
  <files>
    - src/dev_workflow/domain/__init__.py
    - src/dev_workflow/domain/models/__init__.py
    - src/dev_workflow/domain/models/jira_ticket.py
    - src/dev_workflow/domain/models/github_pr.py
  </files>
  <read_first>
    - pyproject.toml
    - src/dev_workflow/__init__.py (verify current package name and imports)
  </read_first>
  <action>
Create the following files with the EXACT content specified:

**src/dev_workflow/domain/__init__.py:**
```python
"""Domain layer — pure business entities and models."""
```

**src/dev_workflow/domain/models/__init__.py:**
```python
"""Domain models — Pydantic models for data contracts."""
from src.dev_workflow.domain.models.jira_ticket import JiraTicket
from src.dev_workflow.domain.models.github_pr import GitHubPR

__all__ = ["JiraTicket", "GitHubPR"]
```

**src/dev_workflow/domain/models/jira_ticket.py:**
```python
"""Jira ticket domain model."""
from pydantic import BaseModel, Field
from enum import Enum


class IssueType(str, Enum):
    STORY = "Story"
    BUG = "Bug"
    TASK = "Task"


class JiraTicket(BaseModel):
    """Domain model representing a Jira ticket."""
    key: str = Field(description="Jira ticket key, e.g. PROJ-123")
    summary: str = Field(description="Ticket title/summary")
    description: str = Field(description="Full ticket description")
    issue_type: IssueType
    status: str
    assignee: str | None = None
    acceptance_criteria: list[str] = Field(default_factory=list)
    comments: list[str] = Field(default_factory=list)
    linked_issues: list[str] = Field(default_factory=list)
```

**src/dev_workflow/domain/models/github_pr.py:**
```python
"""GitHub Pull Request domain model."""
from pydantic import BaseModel, Field


class GitHubPR(BaseModel):
    """Domain model representing a GitHub Pull Request."""
    number: int
    title: str
    body: str
    head_branch: str
    base_branch: str = "main"
    status: str
    url: str
    ticket_key: str | None = Field(default=None, description="Jira ticket key if linked")
```

Create any missing intermediate __init__.py files. Ensure the package path matches `src/dev_workflow/` layout from pyproject.toml.
  </action>
  <verify>
    <automated>
      python3 -c "from src.dev_workflow.domain.models import JiraTicket, GitHubPR; t = JiraTicket(key='TEST-1', summary='x', description='y', issue_type='Story', status='Open'); print(t.model_dump_json())"
    </automated>
  </verify>
  <done>JiraTicket and GitHubPR Pydantic models exist, importable from src.dev_workflow.domain.models</done>
  <acceptance_criteria>
    - `from src.dev_workflow.domain.models import JiraTicket` succeeds without error
    - `from src.dev_workflow.domain.models import GitHubPR` succeeds without error
    - JiraTicket has fields: key, summary, description, issue_type, status, assignee, acceptance_criteria, comments, linked_issues
    - GitHubPR has fields: number, title, body, head_branch, base_branch, status, url, ticket_key
    - Both models are Pydantic BaseModel subclasses
    - All __init__.py files exist in domain/models/ path
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: Create infrastructure adapter stubs</name>
  <files>
    - src/dev_workflow/infrastructure/__init__.py
    - src/dev_workflow/infrastructure/adapters/__init__.py
    - src/dev_workflow/infrastructure/adapters/jira_adapter.py
    - src/dev_workflow/infrastructure/adapters/github_adapter.py
    - src/dev_workflow/infrastructure/adapters/logging_adapter.py
  </files>
  <read_first>
    - src/dev_workflow/domain/models/jira_ticket.py
    - src/dev_workflow/domain/models/github_pr.py
    - src/dev_workflow/ports/__init__.py (will be created by Plan 02 — import by string path, stub here)
  </read_first>
  <action>
Create the following infrastructure adapter stub files. Each adapter must import from domain models (created in task 1) and implement the corresponding port protocol from src.dev_workflow.ports (stub import is fine — will be wired in Plan 02).

**src/dev_workflow/infrastructure/__init__.py:**
```python
"""Infrastructure layer — external service adapters."""
```

**src/dev_workflow/infrastructure/adapters/__init__.py:**
```python
"""Infrastructure adapters — implementations of port protocols."""
from src.dev_workflow.infrastructure.adapters.jira_adapter import JiraAdapter
from src.dev_workflow.infrastructure.adapters.github_adapter import GitHubAdapter
from src.dev_workflow.infrastructure.adapters.logging_adapter import LoggingAdapter

__all__ = ["JiraAdapter", "GitHubAdapter", "LoggingAdapter"]
```

**src/dev_workflow/infrastructure/adapters/jira_adapter.py:**
```python
"""Jira Cloud REST API adapter — implements JiraPort protocol.

To be fully implemented in Phase 3. This stub provides the class
signature and docstring so other modules can import it now.
"""
from src.dev_workflow.domain.models.jira_ticket import JiraTicket


class JiraAdapter:
    """Jira Cloud REST API adapter.

    Implements JiraPort protocol. Full implementation with httpx
    client, rate limiting, and pagination in Phase 3.

    Attributes:
        base_url: Jira Cloud instance URL
        email: Auth email
        api_token: Jira API token
    """

    def __init__(self, base_url: str, email: str, api_token: str) -> None:
        self.base_url = base_url.rstrip("/")
        self.email = email
        self.api_token = api_token

    def fetch_ticket(self, ticket_key: str) -> JiraTicket:
        """Fetch a Jira ticket by key.

        Args:
            ticket_key: Jira ticket key, e.g. 'PROJ-123'

        Returns:
            JiraTicket domain model

        Raises:
            NotImplementedError: Full implementation in Phase 3
        """
        raise NotImplementedError("JiraAdapter.fetch_ticket() implemented in Phase 3")

    def fetch_ticket_comments(self, ticket_key: str) -> list[str]:
        """Fetch comments for a Jira ticket."""
        raise NotImplementedError("JiraAdapter.fetch_ticket_comments() implemented in Phase 3")

    def fetch_linked_issues(self, ticket_key: str) -> list[str]:
        """Fetch linked issues for a Jira ticket."""
        raise NotImplementedError("JiraAdapter.fetch_linked_issues() implemented in Phase 3")
```

**src/dev_workflow/infrastructure/adapters/github_adapter.py:**
```python
"""GitHub adapter — implements GitHubPort protocol.

To be fully implemented in Phase 8. This stub provides the class
signature and docstring so other modules can import it now.
"""
from src.dev_workflow.domain.models.github_pr import GitHubPR


class GitHubAdapter:
    """GitHub CLI/API adapter.

    Implements GitHubPort protocol. Full implementation with gh CLI
    and PR creation in Phase 8.

    Attributes:
        owner: GitHub repository owner
        repo: GitHub repository name
    """

    def __init__(self, owner: str, repo: str) -> None:
        self.owner = owner
        self.repo = repo

    def create_pr(
        self,
        title: str,
        body: str,
        head_branch: str,
        base_branch: str = "main",
    ) -> GitHubPR:
        """Create a GitHub Pull Request.

        Args:
            title: PR title
            body: PR body description
            head_branch: Source branch name
            base_branch: Target branch name (default: main)

        Returns:
            GitHubPR domain model

        Raises:
            NotImplementedError: Full implementation in Phase 8
        """
        raise NotImplementedError("GitHubAdapter.create_pr() implemented in Phase 8")

    def get_pr_status(self, pr_number: int) -> str:
        """Get the status of a Pull Request by number."""
        raise NotImplementedError("GitHubAdapter.get_pr_status() implemented in Phase 8")
```

**src/dev_workflow/infrastructure/adapters/logging_adapter.py:**
```python
"""Logging adapter for structured execution logs.

Provides structured logging that writes to logs/ directory.
To be fully implemented alongside audit requirements (AUDI-01, AUDI-02, AUDI-03).
"""
import logging
from pathlib import Path


class LoggingAdapter:
    """Structured logging adapter.

    Writes timestamped logs to logs/{ticket_key}_{timestamp}.log
    as specified by AUDI-03. Full implementation in Phase 3.

    Attributes:
        log_dir: Directory for log files (default: logs/)
    """

    def __init__(self, log_dir: str | None = None) -> None:
        self.log_dir = Path(log_dir) if log_dir else Path("logs")
        self.log_dir.mkdir(parents=True, exist_ok=True)

    def get_logger(self, ticket_key: str) -> logging.Logger:
        """Get a logger for a specific ticket.

        Args:
            ticket_key: Jira ticket key for log file naming

        Returns:
            Configured logging.Logger instance
        """
        raise NotImplementedError("LoggingAdapter.get_logger() implemented in Phase 3")
```

Create any missing intermediate __init__.py files along the path.
  </action>
  <verify>
    <automated>
      python3 -c "from src.dev_workflow.infrastructure.adapters import JiraAdapter, GitHubAdapter, LoggingAdapter; print('imports ok')"
    </automated>
  </verify>
  <done>Infrastructure adapters importable, each raises NotImplementedError with descriptive message</done>
  <acceptance_criteria>
    - `from src.dev_workflow.infrastructure.adapters import JiraAdapter` succeeds
    - `from src.dev_workflow.infrastructure.adapters import GitHubAdapter` succeeds
    - `from src.dev_workflow.infrastructure.adapters import LoggingAdapter` succeeds
    - JiraAdapter has fetch_ticket, fetch_ticket_comments, fetch_linked_issues methods
    - GitHubAdapter has create_pr and get_pr_status methods
    - LoggingAdapter has get_logger method
    - All methods raise NotImplementedError when called
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: Create mirrored test structure for domain and infrastructure</name>
  <files>
    - tests/__init__.py
    - tests/unit/__init__.py
    - tests/unit/domain/__init__.py
    - tests/unit/domain/models/__init__.py
    - tests/unit/domain/models/test_jira_ticket.py
    - tests/unit/infrastructure/__init__.py
    - tests/unit/infrastructure/adapters/__init__.py
  </files>
  <read_first>
    - src/dev_workflow/domain/models/jira_ticket.py
    - src/dev_workflow/infrastructure/adapters/jira_adapter.py
  </read_first>
  <action>
Create a test structure that mirrors src/ per the testing decision in CONTEXT.md:
- Test organization: By feature — mirrors src/ structure
- Coverage threshold: 80%

**tests/__init__.py:**
```python
"""Test package for dev_workflow."""
```

**tests/unit/__init__.py:**
```python
"""Unit tests for dev_workflow."""
```

**tests/unit/domain/__init__.py:**
```python
"""Domain layer unit tests."""
```

**tests/unit/domain/models/__init__.py:**
```python
"""Domain models unit tests."""
```

**tests/unit/domain/models/test_jira_ticket.py:**
```python
"""Unit tests for JiraTicket domain model."""
import pytest
from src.dev_workflow.domain.models.jira_ticket import JiraTicket, IssueType


class TestJiraTicket:
    """Test suite for JiraTicket Pydantic model."""

    def test_create_minimal_ticket(self):
        """Should create a JiraTicket with required fields only."""
        ticket = JiraTicket(
            key="PROJ-123",
            summary="Implement login feature",
            description="Add login functionality",
            issue_type=IssueType.STORY,
            status="To Do",
        )
        assert ticket.key == "PROJ-123"
        assert ticket.summary == "Implement login feature"
        assert ticket.issue_type == IssueType.STORY

    def test_create_ticket_with_optional_fields(self):
        """Should create a JiraTicket with all optional fields."""
        ticket = JiraTicket(
            key="PROJ-456",
            summary="Fix bug",
            description="Fix login bug",
            issue_type=IssueType.BUG,
            status="In Progress",
            assignee="dev@example.com",
            acceptance_criteria=["Login works", "No errors in console"],
            comments=["First comment"],
            linked_issues=["PROJ-100"],
        )
        assert ticket.assignee == "dev@example.com"
        assert len(ticket.acceptance_criteria) == 2
        assert "PROJ-100" in ticket.linked_issues

    def test_ticket_serialization(self):
        """Should serialize JiraTicket to JSON."""
        ticket = JiraTicket(
            key="PROJ-789",
            summary="Test",
            description="Test ticket",
            issue_type=IssueType.TASK,
            status="Done",
        )
        json_str = ticket.model_dump_json()
        assert "PROJ-789" in json_str
        assert "Test" in json_str

    def test_issue_type_enum_values(self):
        """Should accept all supported issue types."""
        for issue_type in [IssueType.STORY, IssueType.BUG, IssueType.TASK]:
            ticket = JiraTicket(
                key="PROJ-1",
                summary="x",
                description="y",
                issue_type=issue_type,
                status="Open",
            )
            assert ticket.issue_type == issue_type
```

**tests/unit/infrastructure/__init__.py:**
```python
"""Infrastructure layer unit tests."""
```

**tests/unit/infrastructure/adapters/__init__.py:**
```python
"""Infrastructure adapters unit tests."""
```

Create all missing intermediate __init__.py files. The test file for JiraTicket tests the model directly without external dependencies — following the decision in CONTEXT.md (Mock external services).
  </action>
  <verify>
    <automated>
      python3 -m pytest tests/unit/domain/models/test_jira_ticket.py -v --tb=short 2>&1 | tail -20
    </automated>
  </verify>
  <done>Tests for JiraTicket model exist and pass</done>
  <acceptance_criteria>
    - tests/unit/domain/models/test_jira_ticket.py exists with at least 4 test cases
    - pytest tests/unit/domain/models/test_jira_ticket.py passes with 0 failures
    - Test file follows pytest convention (test_* prefix)
    - tests/ directory mirrors src/ structure exactly
    - All intermediate __init__.py files exist
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {src → external services} | Domain models and adapters don't call external services yet — stubs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | test files | accept | Test files are local-only, no external injection risk |
| T-01-02 | Information Disclosure | logging_adapter | mitigate | LoggingAdapter stub defers implementation; real version in Phase 3 will validate log path to prevent path traversal |
</threat_model>

<verification>
1. Run: `python3 -c "from src.dev_workflow.domain.models import JiraTicket, GitHubPR; print('domain models ok')"`
2. Run: `python3 -c "from src.dev_workflow.infrastructure.adapters import JiraAdapter, GitHubAdapter, LoggingAdapter; print('adapters ok')"`
3. Run: `python3 -m pytest tests/unit/domain/models/test_jira_ticket.py -v --tb=short`
4. Verify all __init__.py files exist with `find src/dev_workflow -name '__init__.py' | sort`
</verification>

<success_criteria>
- [ ] src/dev_workflow/domain/models/jira_ticket.py and github_pr.py exist with Pydantic models
- [ ] src/dev_workflow/infrastructure/adapters/ has JiraAdapter, GitHubAdapter, LoggingAdapter stubs
- [ ] All __init__.py files exist in domain/models/ and infrastructure/adapters/ paths
- [ ] tests/unit/domain/models/test_jira_ticket.py exists and pytest passes
- [ ] No external service imports in domain models (dependency inversion)
- [ ] git status shows new files in src/dev_workflow/ and tests/
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-structure/01-01-SUMMARY.md`
</output>
