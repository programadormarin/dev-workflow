---
phase: 02-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dev_workflow/infrastructure/config.py
  - src/dev_workflow/infrastructure/auth.py
  - src/dev_workflow/domain/models/credentials.py
  - tests/unit/test_credentials.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - User can set JIRA_EMAIL, JIRA_API_TOKEN, JIRA_URL environment variables and system loads them
    - System verifies gh CLI authentication before any PR operations
    - System validates all required environment variables on startup with clear error messages
    - No hardcoded credentials exist anywhere in the codebase
  artifacts:
    - path: src/dev_workflow/infrastructure/config.py
      provides: Environment variable loading and validation
    - path: src/dev_workflow/infrastructure/auth.py
      provides: GitHub CLI authentication verification
    - path: src/dev_workflow/domain/models/credentials.py
      provides: Credential domain models
  key_links:
    - from: src/dev_workflow/infrastructure/config.py
      to: src/dev_workflow/domain/models/credentials.py
      via: Pydantic model validation
    - from: src/dev_workflow/infrastructure/auth.py
      to: gh CLI
      via: subprocess.check_output(['gh', 'auth', 'status'])
---

<objective>
Implement authentication module that loads and validates Jira and GitHub credentials from environment variables.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Phase Requirements (AUTH-*):
- AUTH-01: System loads Jira credentials from environment variables (JIRA_EMAIL, JIRA_API_TOKEN, JIRA_URL)
- AUTH-02: System verifies gh CLI authentication before PR operations
- AUTH-03: System validates all required environment variables on startup with clear error messages
- AUTH-04: No hardcoded credentials anywhere in codebase
</context>

<tasks>

<task type="auto">
  <name>task 1: Create credential domain models</name>
  <files>src/dev_workflow/domain/models/credentials.py</files>
  <action>
Create Pydantic models for credentials:
- JiraCredentials: email (str), api_token (str), url (str) with field validation
- GitHubCredentials: verified (bool) - verified via gh CLI
- ConfigLoader: main class that loads and validates all credentials

Ensure all fields use pydantic.Field with proper validators. No sensitive data in model repr.
  </action>
  <verify>
<automated>python -c "from src.dev_workflow.domain.models.credentials import JiraCredentials, ConfigLoader; print('Models import OK')"</automated>
  </verify>
  <done>JiraCredentials and ConfigLoader Pydantic models exist with proper validation</done>
</task>

<task type="auto">
  <name>task 2: Implement environment variable loader</name>
  <files>src/dev_workflow/infrastructure/config.py</files>
  <read_first>src/dev_workflow/domain/models/credentials.py</read_first>
  <action>
Create ConfigLoader in infrastructure layer:
- Load JIRA_EMAIL, JIRA_API_TOKEN, JIRA_URL from os.environ
- Validate all required env vars are present
- Raise ConfigurationError with clear message if any var missing (not generic)
- Support validation at initialization or lazy-load per operation (per discuss-phase: planner decides)

Create custom exception: ConfigurationError with detailed message showing exactly which env var is missing.
  </action>
  <verify>
<automated>python -c "
import os
os.environ['JIRA_EMAIL'] = 'test@example.com'
os.environ['JIRA_API_TOKEN'] = 'testtoken'
os.environ['JIRA_URL'] = 'https://test.atlassian.net'
from src.dev_workflow.infrastructure.config import ConfigLoader
loader = ConfigLoader()
print('Config loader works')
"</automated>
  </verify>
  <done>ConfigLoader loads JIRA_EMAIL, JIRA_API_TOKEN, JIRA_URL from environment with clear error messages</done>
</task>

<task type="auto">
  <name>task 3: Implement GitHub CLI verification</name>
  <files>src/dev_workflow/infrastructure/auth.py</files>
  <read_first>src/dev_workflow/infrastructure/config.py</read_first>
  <action>
Create GitHubAuth in infrastructure layer:
- Verify gh CLI is installed and authenticated via subprocess
- Run `gh auth status` and check for "Logged in" or similar success indicators
- Return GitHubCredentials with verified=True on success
- Raise clear error if gh not installed or not logged in

This is called by ConfigLoader or used directly before PR operations (per AUTH-02).
  </action>
  <verify>
<automated>python -c "
from src.dev_workflow.infrastructure.auth import GitHubAuth
auth = GitHubAuth()
result = auth.verify()
print(f'GH verified: {result.verified}')
" 2>&1 | head -5</automated>
  </verify>
  <done>GitHubAuth.verify() checks gh CLI authentication status</done>
</task>

<task type="auto">
  <name>task 4: Create unit tests for credentials</name>
  <files>tests/unit/test_credentials.py</files>
  <read_first>src/dev_workflow/domain/models/credentials.py</read_first>
  <action>
Create pytest tests:
- test_jira_credentials_valid: valid email/token/url passes validation
- test_jira_credentials_missing_field: missing field raises ValidationError
- test_config_loader_loads_env_vars: loads from environment correctly
- test_config_loader_missing_env: clear error message when env var missing
- test_github_auth_verified: successful gh auth returns verified=True
- test_github_auth_not_logged_in: clear error when not logged in
- test_no_hardcoded_secrets: grep for hardcoded credentials in codebase

Ensure tests mock subprocess for gh CLI testing.
  </action>
  <verify>
<automated>pytest tests/unit/test_credentials.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All credential tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment → config loader | Untrusted env vars validated at boundary |
| config → domain models | Validated data enters domain layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Credentials in logs | mitigate | Ensure no credentials in repr/str output |
| T-02-02 | Tampering | Env var injection | accept | User controls their own env, not a threat |
| T-02-03 | Denial of Service | Missing env vars | mitigate | Clear error messages, not crash |
</threat_model>

<verification>
- [ ] All 4 AUTH-* requirements addressed
- [ ] Tests run and pass
- [ ] No hardcoded secrets (grep verification)
</verification>

<success_criteria>
- ConfigLoader loads JIRA_EMAIL, JIRA_API_TOKEN, JIRA_URL
- GitHubAuth verifies gh CLI authentication
- Clear error messages on validation failure (not generic)
- No hardcoded credentials in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-01-SUMMARY.md`
</output>